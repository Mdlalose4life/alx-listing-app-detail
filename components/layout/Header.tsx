import React from 'react';
import CombinedHeader from '../header-components/Combined-header';

const Header: React.FC = () => {
  return (
    <header>
      <CombinedHeader/>
    </header>
  );
};

export default Header;